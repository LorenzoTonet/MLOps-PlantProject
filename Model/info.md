## Model

### Intro

This folder contains the scripts for training, predicting and detecting drift

### Training

Run the file train.py to train the NHITS model on the current WandB run.

```bash
python -m Model.train
```

### Predicting

Inside of the file model.py there is the function predict used for making predictions.

### Drift detection

Inside of the file xxxx.py